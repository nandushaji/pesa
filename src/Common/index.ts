import { Module } from "@nestjs/common";
import { AbstractEntity } from "./AbstractEntity";

export default {
  AbstractEntity
}

@Module({
    imports: [
      
    ],
  })
  export class CommonModule {}
  